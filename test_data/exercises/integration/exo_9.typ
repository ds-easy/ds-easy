  #question[
    On donne la fonction $g$ définie sur $[1; e]$ par $g(x) = (ln(x))^2$.
    a) Déterminez une primitive de la fonction $x arrow 2ln(x)/x$ sur $[1; e]$.
    b) En déduire $integral_1^e (ln(x))^2 \dx$
    
    #v(4cm)
  ]
